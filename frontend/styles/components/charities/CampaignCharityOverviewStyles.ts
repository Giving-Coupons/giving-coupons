import { SxProps } from '@mui/material';
import { theme } from '../../../utils/theme';

export const accordionStackSx: SxProps = {
  borderRadius: '20px',
  boxShadow: `0px 0px 20px 5px ${theme.palette.neutral.light}`,
};
